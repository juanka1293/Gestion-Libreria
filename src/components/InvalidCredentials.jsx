import React from 'react';
import './css/InvalidCredentials.css'; // Asegúrate de importar tus estilos CSS

const InvalidCredentialsMessage = () => {
  return (
    <div className="invalid-credentials-message">
      <p>Credenciales Inválidas</p>
    </div>
  );
};

export default InvalidCredentialsMessage;
